// This test was removed after reverting the always-visible title input behavior.
// Keeping a skipped placeholder to avoid re-introduction without intent.
describe.skip("TemplateSelector retry preserves entered title (removed scenario)", () => {
  test("placeholder", () => {
    expect(true).toBe(true);
  });
});
