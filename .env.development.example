# Frontend development environment overrides
# Copy to .env.development.local to customize

# Base URL for API (defaults to proxy in package.json if unset)
# REACT_APP_API_BASE=http://localhost:8000

# Alternate port example:
# REACT_APP_API_BASE=http://localhost:8100
